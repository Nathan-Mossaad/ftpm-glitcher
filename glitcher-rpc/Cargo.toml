[package]
name = "glitcher-rpc"
version = "0.1.0"
edition = "2024"

[features]
mcu = ["defmt", "postcard/use-defmt"]

[dependencies]
serde = {version= "1.0", default-features = false, features=["serde_derive"]}
postcard = "1.1"

# MCU specific libaries
defmt = {version="1.0.1", optional = true}
